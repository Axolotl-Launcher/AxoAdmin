import { createRemoteJWKSet, jwtVerify } from "jose";
export type AdminSession={identity:{name:string;email:string|null};organization:string;logoutUrl:string};
export async function getAdminSession(headers:Headers):Promise<AdminSession>{if(process.env.NODE_ENV==="development"&&process.env.AXOADMIN_MOCK_AUTH==="true")return {identity:{name:"本地开发身份",email:"dev@example.com"},organization:"axolotl-launcher",logoutUrl:"/cdn-cgi/access/logout"};const token=headers.get("cf-access-jwt-assertion");const team=process.env.CF_ACCESS_TEAM_DOMAIN;const audience=process.env.CF_ACCESS_AUDIENCE;if(!token||!team||!audience)throw new Error("ADMIN_AUTH_UNCONFIGURED");const issuer="https://"+team+".cloudflareaccess.com";const {payload}=await jwtVerify(token,createRemoteJWKSet(new URL(issuer+"/cdn-cgi/access/certs")),{issuer,audience,algorithms:["RS256"]});const email=typeof payload.email==="string"?payload.email:null;const name=typeof payload.name==="string"?payload.name:(email??"GitHub member");return {identity:{name,email},organization:"axolotl-launcher",logoutUrl:"/cdn-cgi/access/logout"};}