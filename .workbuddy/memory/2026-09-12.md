# 2026-09-12

## git 仓库 .git 消失事故与恢复

- 用户要求舍弃所有本地更改、对齐远端。执行 `git stash push -u` 时报 `Unable to create .git/index.lock: No such file or directory`，随后确认 **整个 `.git` 目录消失**（Bash 与 PowerShell 双环境验证），原因不明（疑似本机 Bash shim/沙箱环境异常）。
- 损失：未推送提交 `8de28bd`（chore: 接入 Prettier 并全量格式化）与备份分支 `backup/pre-align-20260912` 均随 `.git` 丢失；工作区源文件完好。
- 恢复方式：原地 `git init -b main` + remote add origin + fetch + `git reset --hard origin/main` + `git clean -fd`（不带 -x，保住 .env.local / node_modules / .next / .workbuddy）。
- 结果：main 与 origin/main 对齐于 `7d60d59`，工作区干净，`.env.local` 完好。
- 教训：**本机执行 git 破坏性操作（stash/reset/clean）前，先确认 `.git` 存在且健康**；本机 Bash shim 缺 coreutils 且环境不稳定，关键操作后立即用 `git status` 验证。
